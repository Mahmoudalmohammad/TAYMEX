# Governed task metadata
Each feature/fix PR must include `.governance/task.yaml`. The implementing agent may request scope expansion but may not self-approve sensitive authority. The file is CODEOWNED.
