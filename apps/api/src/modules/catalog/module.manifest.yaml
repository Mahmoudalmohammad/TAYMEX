id: catalog
layer: domain
owner: taymex
sourceRoots: [apps/api/src/modules/catalog]
tags: [layer:domain, domain:catalog]
dependencies:
  allowedModules: []
  allowedTags: [layer:kernel, layer:foundation]
data:
  owns: []
  readsViaContracts: []
  classification: internal
  adapter: none
  schemaRoots: []
settings:
  defines: [catalog.products.defaultPageSize]
  consumes: [catalog.products.defaultPageSize]
permissions:
  defines: [catalog.products.read, catalog.products.manage]
  consumes: [catalog.products.read, catalog.products.manage]
events:
  emits: []
  consumes: []
contracts:
  owns: []
  consumes: []
api:
  internalOnly: false
securityTags: [authorization, audit]
performanceTags: [bounded-list]
extensionModes: []
