# Activation requirement
Before this repository is considered merge-protected, configure the default-branch ruleset to require:
- pull requests;
- code-owner approval;
- `trust-root` status;
- `governance-summary` status;
- no force push / branch deletion;
- no ordinary agent bypass.
